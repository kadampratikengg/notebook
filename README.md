(Quick start)

- Server: copy `server/.env.example` to `server/.env` and set `MONGO_URI` and `JWT_SECRET`.
- Client: create `client/.env` or use the included `client/.env` — ensure `REACT_APP_API_URL` points to your backend, e.g. `http://localhost:5000/api`.
